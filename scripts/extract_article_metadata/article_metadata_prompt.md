请严格使用系统提示中给出的 JSON schema。当前输入是论文 Markdown 的一个章节块；只提取该块中可定位的文章身份、组织来源、神经相关细胞和单细胞分析统计方法。对于跨章节才能确认的事项，只保留当前块能支持的最小结论。不要把普通差异表达基因、讨论中的细胞或软件默认参数写入结果。所有证据都必须带 source_locator 和短 evidence_snippet；没有证据时返回空数组，不要猜测。
